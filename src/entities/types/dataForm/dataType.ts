type dataType = {
    data: {
        firstName: string,
        companyName?: string,
        phone: string,
        mail: string,
        projectBugdet?: string,
        description?: string,
        attachment?: string | number,
    }
}

export type {dataType}